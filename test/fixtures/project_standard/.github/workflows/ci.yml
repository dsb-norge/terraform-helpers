name: CI
on: [push]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Terraform
        with:
          terraform-version: "1.6.5"
          tflint-version: "v0.54.0"
